# Asset notice

`proofbench.csv` and the rubric preserved in `grader_prompt.txt` were copied
from the repository's vendored `third_party/HyperAgents` snapshot when this
experiment was made self-contained. They are retained as immutable benchmark
inputs, not as HyperAgents runtime or evaluation code.

See `LICENSE.md` in this directory for the accompanying CC BY-NC-SA 4.0
license text. The grader source itself was not copied; `grader_prompt.txt`
contains only the frozen prompt consumed by this experiment.
