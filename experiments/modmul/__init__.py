"""SAIR modular arithmetic challenge — task package (WS-1).

Top-level consumer package, parallel to the framework packages. Depends on
evoserve (eval-side SDK) + evoguard (Sandbox); the framework never imports
this package (enforced by tests/test_layering.py).
"""
