-- The seed submits nothing usable on purpose: it exists so the workspace has
-- the file the grader looks for, and so a run that changes nothing scores 0
-- rather than erroring. Replace the whole body.
import JudgeProblem

def submission : Goal := by
  sorry
